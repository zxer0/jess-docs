$('.indent_spec').bind('ajax:success', function() {
    $('.s<%=@spec.id%>').fadeOut();
    $('.s<%=@spec.id%>').remove();
    $('.s<%=@closest_older_sibling_id%>').replaceWith("<%= j(render partial: "specs", collection: @print_specs_hash, as: :spec) %>");
});